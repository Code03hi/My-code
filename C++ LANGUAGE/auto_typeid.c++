#include <bits/stdc++.h>
using namespace std;

int main()
{
    auto id = 3;
    auto name = "Bhagwan Jha";
    auto Year = "2nd Graduate ";
    cout << typeid(id).name() << endl;
    cout << typeid(name).name() << endl;
    cout << typeid(Year).name() << endl;
    return 0;
}